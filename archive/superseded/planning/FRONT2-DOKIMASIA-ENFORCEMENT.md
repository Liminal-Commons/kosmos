# Front 2: Dokimasia Enforcement

**SUPERSEDED** by `genesis/chora-dev/PROMPT-DOKIMASIA-ENFORCEMENT.md`.

That prompt removes the `off` mode (validation always runs), targets `strict` as default, and adds integration notes with manifest validation and the reconciler engine.
