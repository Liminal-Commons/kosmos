# Credentials Typos — External Service Credentials
#
# Typos for the credentials domain.

entities:

# =============================================================================
# CREDENTIALS
# =============================================================================

- eidos: typos
  id: typos-def-credential-attainment
  data:
    name: credential-attainment
    description: "Compose a credential-attainment entity (dynamic capability from unlocked credential)"
    target_eidos: credential-attainment
